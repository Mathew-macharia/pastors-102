# Copy this file to `.env` and fill in your real Helius API key.
# Get a free key at https://dashboard.helius.dev/  (Sender works on the free tier.)

HELIUS_API_KEY=

# Optional overrides — CLI flags take precedence if both are set.
# WALLET_MIN_AMOUNT_SOL=0.09
# WALLET_MAX_AMOUNT_SOL=0.11
# WALLET_MIN_DELAY=30
# WALLET_MAX_DELAY=300
# WALLET_PRIO_MIN=50000
# WALLET_PRIO_MAX=200000
